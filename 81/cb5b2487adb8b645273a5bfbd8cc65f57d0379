{"category": "Removed", "changelog_entry": "Simplified demo scripts by eliminating duplicated status-reporting logic and dead code, resulting in ~20 lines of reduced shell complexity.", "summary": "The duplicated status-reporting logic in demo scripts has been consolidated into a single `get_status()` function in `_common.sh`. This change eliminates ~20 lines of duplicated shell logic and dead code, making the code more maintainable. The updated function now calls `kubectl` commands only when it is available, ensuring compatibility with environments without Kubernetes installed.", "version": 1}
