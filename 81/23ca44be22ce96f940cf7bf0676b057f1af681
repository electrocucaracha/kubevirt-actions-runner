{"category": "Fixed", "changelog_entry": "Resolved an issue where GitHub Actions workflows relying on write access to artifact metadata were failing due to missing permissions by updating the workflow's permissions accordingly and requiring users to review their existing workflows for necessary updates.", "summary": "The permissions for the GitHub Actions workflow were updated to remove write access to artifact metadata, which may break workflows relying on this feature. This change does not affect API or CLI contracts and has no security implications. Users must review their existing workflows and update them if necessary to avoid errors due to missing permissions.", "version": 1}
