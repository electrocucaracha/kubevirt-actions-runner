Category: Added

A GitHub Actions workflow for building and publishing Docker images has been enabled, allowing automated creation of Docker containers. The new workflow triggers on push to the master branch, tags with version numbers, and pull requests to the master branch. It uses various actions to log in to the container registry, extract metadata, build and push the image, and generate an artifact attestation. This change requires no migration steps or breaking behavior changes.
