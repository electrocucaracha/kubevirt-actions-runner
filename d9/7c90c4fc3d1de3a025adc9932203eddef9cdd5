{"category": "Changed", "changelog_entry": "Updated version dependencies in GitHub Actions workflows to 6.x for several packages and pre-commit configuration to include new repositories and hooks.", "summary": "The version dependencies in the repository's GitHub Actions workflows have been updated from 5.x to 6.x for several packages, including `actions/checkout`, `actions/setup-go`, and others. The pre-commit configuration file has also been updated with new repositories and hooks. Additionally, a script now installs uvx if it's not available on the system before updating the pre-commit configuration using uvx.", "version": 1}
