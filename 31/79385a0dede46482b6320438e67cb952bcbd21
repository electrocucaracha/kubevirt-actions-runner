{"category": "Fixed", "changelog_entry": "Resolved the zizimor linting issue by persisting credentials in the actions/checkout step and updating the sh-checker action, without introducing breaking behavior or requiring migration steps.", "summary": "The GitHub Actions workflow for on-demand CI was modified to address a linting issue, specifically by adding an option to persist credentials in the actions/checkout step and updating the sh-checker action. This change does not introduce breaking behavior or require migration steps. The config schema changes include modifying the checkout step with persist-credentials set to false.", "version": 1}
