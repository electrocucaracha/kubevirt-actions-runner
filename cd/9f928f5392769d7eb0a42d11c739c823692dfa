Category: Added

The custom wordlist has been updated to include missing terms, specifically "bugfixes" and "cecf", which were causing false positives in spelling checks. This change ensures smoother CI runs and reduces noise for contributors by accurately filtering out these words from linting checks. No other changes were made to the wordlist.
