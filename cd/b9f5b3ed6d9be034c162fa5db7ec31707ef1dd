Category: Removed

The unit tests for the delete resources use case have been added, introducing breaking behavior in the `DeleteResources` method of the `Runner` interface and its implementations. The API contract has changed to require two string arguments: the virtual machine instance name and data volume name. This change affects users who rely on the previous implementation.
