{"category": "Changed", "changelog_entry": "Updated the Docker build environment to utilize the newer git package version 2.49.1-r0, potentially necessitating users who have pinned dependencies to review and update their configurations accordingly.", "summary": "The Dockerfile has been updated to use a newer version of the git package, changing from 2.49.0-r0 to 2.49.1-r0 when setting up the build environment. This change may require users who have pinned their dependencies to review and update their configurations if necessary. The migration step is not explicitly required but should be considered for consistency.", "version": 1}
