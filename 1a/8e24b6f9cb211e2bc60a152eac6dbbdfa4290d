{"category": "Changed", "changelog_entry": "The cleanup process now continues even if it encounters non-existent runner instances and data volumes, allowing for smoother deletion operations without interruption.", "summary": "The cleanup process now ignores errors when deleting runner instances and data volumes, allowing the deletion to continue even if one of these resources does not exist. This change affects migration requirements for users who previously relied on error propagation during cleanup. The API contract remains unchanged, but config schema changes are not relevant here.", "version": 1}
