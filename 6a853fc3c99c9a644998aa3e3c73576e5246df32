{"category": "Added", "changelog_entry": "Enabled configurable wait timeout for terminal VMI phase, introducing Running+Ready milestone that ends wait without requiring Succeeded phase, with default timeout of 10 minutes adjustable via KAR_WAIT_TIMEOUT environment variable.", "summary": "The `KAR_WAIT_TIMEOUT` environment variable is now configurable, allowing users to set the maximum time to wait for a terminal VMI phase (`Succeeded`/`Failed`). This change also introduces a new milestone: Running+Ready, which ends the wait without requiring Succeeded phase. The default timeout is 10 minutes, but can be adjusted with `KAR_WAIT_TIMEOUT`.", "version": 1}
