{"category": "Fixed", "changelog_entry": "Stabilized the runner's behavior against temporary Kubernetes API issues by automatically reconnecting the VMI watch on stream closure without breaking any existing functionality.", "summary": "The runner now reconnects the VMI watch when the stream closes due to a timeout, avoiding treating API-server timeouts as job failures. This change affects wait and cleanup timeouts for VM-backed jobs, which can be configured in the documentation. The primary user outcome is improved resilience against temporary Kubernetes API issues.", "version": 1}
