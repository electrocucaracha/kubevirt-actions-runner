{"category": "Changed", "changelog_entry": "Updated the linter configuration in `.golangci.yml` to require explicit rule settings and package-specific `gomoddirectives`, necessitating migration steps for customized configurations.", "summary": "The GoCI-lint version was upgraded, introducing breaking changes to the linter configuration in `.golangci.yml`. The `enable-all` option is replaced with `default: all`, and several rules are updated. Additionally, the `gomoddirectives` settings have been modified to allow specific packages. This update may require migration steps for users who had customized their configurations.", "version": 1}
