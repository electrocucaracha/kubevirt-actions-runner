Category: Removed

Removed duplicated code in test functions, simplifying the testing process by introducing a new helper function `verifyLoggerImpl` to reset the singleton for testing. This change does not introduce any breaking behavior, API or CLI changes, security impact, or config schema changes. The primary outcome is improved maintainability and reduced duplication in test code.
