{"category": "Changed", "changelog_entry": "Optimized textlint and prettier usage by switching to local devDependencies, eliminating the need for global installations and ensuring consistent versions across developer machines and CI.", "summary": "The Makefile now uses local npm packages for textlint and prettier, ensuring consistent versions and avoiding permission issues across developer machines and CI. This change eliminates the need for global installations, improving reproducibility and reliability. The Makefile now invokes local binaries with npx --no-install, and devDependencies are installed with npm install --save-dev.", "version": 1}
