Category: Changed

The Dockerfile has been modified to change the working directory from `/go` to `/app`, add git version 2.49.0, and update the build command to use `-buildvcs=true`. This change affects how the application is built within the container, potentially requiring users to adjust their build scripts or configurations if they rely on specific behavior in the Dockerfile.
