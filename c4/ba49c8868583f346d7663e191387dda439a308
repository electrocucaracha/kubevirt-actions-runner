{"category": "Changed", "changelog_entry": "Enabled successful linter validation on Ubuntu platforms by installing OpenSSL libraries prior to running the linter in the CI workflow.", "summary": "The installation of OpenSSL was added before running the linter in the CI workflow for Ubuntu platforms to ensure successful validation without errors. This change addresses an issue where the default Ubuntu image lacked required OpenSSL libraries, causing build and test failures on these environments. The updated workflow now includes a separate step to install OpenSSL, improving consistency across supported platforms.", "version": 1}
