{"category": "Added", "changelog_entry": "Optimized the textlint process by excluding the custom term list from linting checks.", "summary": "The `.github/.wordlist.txt` file, containing custom terms, is now excluded from textlint checks to prevent unnecessary lint errors and keep the process focused on relevant content. This change adds the file to the `.textlintignore` list, ensuring it is not linted. No other files are affected by this change, and the textlint process remains unchanged.", "version": 1}
