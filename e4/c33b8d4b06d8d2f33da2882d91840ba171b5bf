{"category": "Changed", "changelog_entry": "Improved validation for the create resources use case by introducing unit tests that verify correct behavior when providing empty values for VM template, runner name, and JIT configuration.", "summary": "Unit tests were added for the create resources use case, including validation of VM template, runner name, and JIT configuration. The tests check for errors when providing empty values for these parameters. The code changes also include updated flag names to follow Go naming conventions (e.g., \"VMTemplate\" instead of \"VmTemplate\").", "version": 1}
