{"category": "Fixed", "changelog_entry": "The GitHub Actions workflow for building the project now persists credentials by default, which may impact users who rely on this behavior and should review their workflows to ensure they handle credential persistence as intended.", "summary": "The GitHub Actions workflow for building the project now persists credentials by default, which may impact users who rely on this behavior. This change does not affect the API or CLI contract and has no security implications. The configuration schema remains unchanged, so no migration steps are required. Users should review their workflows to ensure they handle credential persistence as intended.", "version": 1}
