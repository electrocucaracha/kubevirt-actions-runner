{"category": "Added", "changelog_entry": "Enabled the Improvers workflow to utilize GitHub Copilot features during execution by adding the COPILOT_TOKEN secret to its workflow secrets.", "summary": "The Improvers workflow now includes the COPILOT_TOKEN secret, enabling the use of GitHub Copilot features during execution. This change allows the workflow to authenticate with Copilot, but it does not introduce any breaking behavior or migration requirements. The workflow's config schema has been updated with the addition of the COPILOT_TOKEN secret.", "version": 1}
