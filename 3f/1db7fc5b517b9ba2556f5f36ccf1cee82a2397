{"category": "Fixed", "changelog_entry": "Stabilized container execution by modifying the Dockerfile to use /bin/ash instead of /bin/sh for SHELL commands, ensuring compatibility in certain environments and potentially requiring adjustments in scripts that rely on this behavior.", "summary": "The Dockerfile has been modified to resolve a linting issue, changing the SHELL command from /bin/sh to /bin/ash. This change affects how the container executes shell commands and may require adjustments in scripts that rely on this behavior. The fix addresses an issue reported in #32, ensuring compatibility with certain environments.", "version": 1}
