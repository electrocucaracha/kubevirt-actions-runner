{"category": "Removed", "changelog_entry": "Simplified configure.sh to no longer depend on the unused _common.sh script, reducing unnecessary complexity and dependencies.", "summary": "configure.sh no longer sources _common.sh, removing an unnecessary dependency between scripts. This change was made because _common.sh was never called, and only functions from _utils.sh were used. The removal of this unused source reduces complexity and dependencies.", "version": 1}
