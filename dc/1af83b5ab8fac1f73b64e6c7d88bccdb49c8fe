{"category": "Changed", "changelog_entry": "Optimized CI workflow efficiency by conditionally running Go-related jobs only when Go files have changed, reducing unnecessary resource usage and build times.", "summary": "Go-related jobs in the CI workflow now only run when Go files have changed, reducing unnecessary resource usage and build times. Previously, these jobs ran on every execution, even with no modified Go files. The publish job logic ensures Go jobs must succeed when Go files change, while still allowing Docker-only changes to trigger publishing. This optimization improves workflow efficiency and minimizes redundant job runs.", "version": 1}
