Category: Changed

The CI update script has been improved, adding a trap to run `make fmt` on exit to maintain code formatting consistency. The script now also correctly handles GitHub Actions commit hashes by sorting and updating them in the workflow file. This change ensures that the build process remains accurate and up-to-date with the latest dependencies.
