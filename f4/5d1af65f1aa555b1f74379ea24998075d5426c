{"category": "Fixed", "changelog_entry": "Resolved issues with integration tests by updating them to reflect changes in cluster configuration and utilizing the newgrp command for log export.", "summary": "The integration tests were updated to reflect changes in the cluster configuration, including enabling emulation for the kubevirt operator and restarting the virt-handler daemonset. The scripts now also use the `newgrp` command to export logs from the k8s cluster instead of mgmt. Additionally, some environment variables are no longer printed during testing.", "version": 1}
