{"category": "Changed", "changelog_entry": "Optimized test concurrency by marking all relevant tests in the appcontext package as parallel, allowing them to run concurrently and reducing overall test suite execution time without altering functional behavior or compromising test reliability.", "summary": "Tests in the appcontext package are now marked as parallel, allowing them to run concurrently and reducing overall test suite execution time. This change updates all relevant tests in appcontext_test.go to use t.Parallel, but does not alter functional behavior or compromise test reliability. No migration steps are required, and there is no security impact or config schema change.", "version": 1}
