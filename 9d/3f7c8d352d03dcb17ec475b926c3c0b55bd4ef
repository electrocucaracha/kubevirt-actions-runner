{"category": "Added", "changelog_entry": "Enabled explicit documentation of intentional default values for cleanup and shutdown timeouts, preventing linter warnings about magic numbers without introducing functional changes.", "summary": "The code now includes explicit comments to suppress linter warnings about magic numbers, specifically for the 5-minute cleanup and 5-second shutdown timeouts. This change documents the intended default values, preventing potential confusion and ensuring consistency. The linter warnings are addressed without introducing functional changes.", "version": 1}
