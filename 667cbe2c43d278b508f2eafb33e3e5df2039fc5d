{"category": "Changed", "changelog_entry": "Enabled comprehensive verification tasks for Copilot CLI by relaxing tool and path restrictions while maintaining security through explicit denial of dangerous shell commands.", "summary": "Copilot CLI's tool and path restrictions have been relaxed to allow for comprehensive verification tasks, while still maintaining security for critical operations by explicitly denying dangerous shell commands. The default now allows all tools, paths, and URLs, with specific denials for rm, sudo, chown, chmod 777, and dd. This change enables greater flexibility for legitimate tool usage but requires further adjustments as new requirements or risks emerge.", "version": 1}
