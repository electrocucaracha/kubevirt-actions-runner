{"category": "Changed", "changelog_entry": "Simplified the evaluation of VMI phases by extracting the shared logic into a single helper function, without introducing any observable changes in behavior.", "summary": "The duplicated VMI phase-evaluation logic in `handleWatchEvent` and `refreshVMIStatus` was extracted into a single `evaluateVMIStatus` helper function to reduce technical debt. This change does not affect behavior, as both functions delegate to the new helper and return the same values as before. The new function handles readiness milestone reporting and phase-transition handling, improving code maintainability.", "version": 1}
