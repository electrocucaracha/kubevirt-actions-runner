{"category": "Changed", "changelog_entry": "Stabilized linting and formatting results across environments by removing stale dependencies and virtual environments through an automated cleanup step prior to running these tasks.", "summary": "Linting and formatting targets in the Makefile now remove temporary files before running, ensuring a clean workspace and more reliable results across environments. This change may increase execution time but improves reproducibility by removing stale dependencies and virtual environments that could interfere with linting and formatting. The cleanup target removes node_modules, .tox, and .venv directories to achieve this outcome.", "version": 1}
