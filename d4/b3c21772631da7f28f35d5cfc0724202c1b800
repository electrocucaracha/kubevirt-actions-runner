{"category": "Fixed", "changelog_entry": "The GH action triggers for workflows have been optimized to only run the `fixers.yml` workflow when an opened issue already includes a matching remediation label.", "summary": "The GH action triggers for workflows have been updated to improve the accuracy of issue-triggered jobs, specifically for Docker build failures and golangci-lint findings. The `fixers.yml` workflow now only runs when an opened issue already includes either the matching remediation label. This change does not break any existing behavior or require migration steps, nor does it impact security or config schema.", "version": 1}
