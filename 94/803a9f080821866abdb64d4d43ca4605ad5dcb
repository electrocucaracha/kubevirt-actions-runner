{"category": "Fixed", "changelog_entry": "Resolved the setup-go cache poisoning vulnerability by requiring explicit Go versions in workflows instead of relying on the cache and removing the vulnerable cache from all relevant configurations.", "summary": "The setup-go cache was disabled to prevent potential poisoning vulnerability. This change affects Go versioning in workflows, now requiring explicit versions instead of relying on the cache. The vulnerable cache has been removed from all relevant workflows. Users must update their workflow configurations accordingly. No security impact is expected beyond this fix.", "version": 1}
