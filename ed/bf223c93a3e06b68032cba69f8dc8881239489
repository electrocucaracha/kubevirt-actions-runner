{"category": "Changed", "changelog_entry": "The super-linter image reference has been updated to ghcr.io from github.com requiring users to re-pull the image if they have previously pulled from the old repository.", "summary": "The super-linter image reference in the Makefile has been updated to use ghcr.io instead of github.com, which may require users to update their local images if they have previously pulled from the old repository. This change does not affect the API or CLI contract and there are no security implications. The migration step for users is to re-pull the super-linter image with the new reference.", "version": 1}
