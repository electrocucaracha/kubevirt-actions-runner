Category: Added

The GitHub Actions workflow for building the project has been updated to include a smoke test for images, which will now be built and tested in addition to Go code. This change introduces a new Docker image tag and requires no migration steps. The primary outcome of this update is improved testing coverage for projects that use Docker images.
