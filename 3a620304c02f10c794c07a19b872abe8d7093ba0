{"category": "Fixed", "changelog_entry": "Resolved permission errors accessing Kubernetes cluster logs in CI environment by running kind export logs command with elevated permissions.", "summary": "The kind export logs command in the CI environment now runs with elevated permissions to resolve permission errors accessing Kubernetes cluster logs, which should reduce missing log artifacts in failed workflow runs. This change involves running the command with sudo and does not introduce any breaking behavior or API changes. The configuration for the on-demand CI job has been updated accordingly.", "version": 1}
