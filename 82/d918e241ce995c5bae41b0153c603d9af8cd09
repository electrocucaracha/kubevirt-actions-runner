Category: Fixed

The Dockerfile now includes the .git directory in the build process, which adds Git information to the image. This change affects users who rely on this information for debugging and development purposes. The migration step involves updating the Dockerfile to include the new COPY instruction.
