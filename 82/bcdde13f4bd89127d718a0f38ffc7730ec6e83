{"category": "Changed", "changelog_entry": "Enabled developers to work with application context by introducing an AppContext struct that stores Virtual Machine Instance and Data Volume names.", "summary": "The application context was implemented, introducing an AppContext struct to store Virtual Machine Instance and Data Volume names. This change removed redundant methods from the KubevirtRunner type and replaced them with calls to the AppContext instance. The NewAppContext function now creates a singleton instance of AppContext, ensuring thread safety through a once.Do block.", "version": 1}
