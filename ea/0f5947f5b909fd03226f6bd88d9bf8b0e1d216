{"category": "Fixed", "changelog_entry": "Resolved the Docker build issue by updating the default shell from \"/bin/bash\" to \"/bin/sh\", which may impact users relying on Bash-specific features during container builds and runs.", "summary": "Docker build issue fixed due to incorrect SHELL command which has been updated from \"/bin/bash\" to \"/bin/sh\". This change affects the default shell used for building and running containers, potentially impacting users who rely on Bash-specific features. No migration steps are required as this is a one-time fix.", "version": 1}
