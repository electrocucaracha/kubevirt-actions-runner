Category: Changed

The versions used in the GitHub workflow for linter validation were updated, specifically switching from super-linter version 7.4.0 to 8.0.0. This change may require manual review of the updated configuration and could potentially introduce breaking behavior if not properly managed during migration. The upgrade does not appear to impact API or CLI contracts, config schema, or security.
