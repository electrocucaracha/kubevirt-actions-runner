{"category": "Changed", "changelog_entry": "The NewRootCommand function now requires users to pass a Runner instance instead of command options, affecting API usage and potentially necessitating migration steps for existing scripts.", "summary": "The NotifyContext method has been replaced in the NewRootCommand function, requiring users to pass a Runner instance instead of command options. This change affects API usage and may require migration steps for existing scripts. The underlying run function now uses the provided Runner instance to create and delete resources. Security implications are unchanged.", "version": 1}
