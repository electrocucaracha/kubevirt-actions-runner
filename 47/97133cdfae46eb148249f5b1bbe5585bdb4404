{"category": "Fixed", "changelog_entry": "Triggering of on-demand CI jobs is now optimized to include all pull requests, including opened, synchronized, and reopened ones, as well as submitted reviews, without introducing any breaking changes that require maintainers to review their setup.", "summary": "The on-demand CI jobs now catch all pull requests, including opened, synchronized, and reopened ones, in addition to submitted reviews. This change affects the behavior of these jobs by triggering them for more scenarios. The workflow configuration has been updated accordingly. No breaking changes are introduced, but maintainers should review their existing setup to ensure compatibility with this new behavior.", "version": 1}
