{"category": "Changed", "changelog_entry": "Reduced the default cleanup timeout to 5 minutes, which may require users to update their scripts or configurations to accommodate the new value and potentially break existing workflows relying on the longer timeout.", "summary": "The default cleanup timeout in the Kar command has been reduced from 30 minutes to 5 minutes. This change may require users to adjust their cleanup processes, potentially breaking existing workflows if they rely on the longer timeout. The API and CLI contract remains unchanged, with no security implications or config schema changes. Users will need to update their scripts or configurations to accommodate the new default timeout value.", "version": 1}
