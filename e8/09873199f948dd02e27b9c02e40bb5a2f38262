{"category": "Removed", "changelog_entry": "Simplified the project configuration by eliminating unnecessary dependencies and shell helper functions, resulting in no observable impact on developers or operators.", "summary": "The dependency on github.com/spf13/viper was removed from the project's configuration, as it was already deleted from the go.mod file, and the lint config still referenced it. Additionally, two unused shell helper functions, debug() and warn(), were removed from the scripts/_utils.sh file, as they were not being called anywhere in the repository. This change does not introduce any breaking behavior or migration requirements, nor does it affect the API or CLI contract.", "version": 1}
