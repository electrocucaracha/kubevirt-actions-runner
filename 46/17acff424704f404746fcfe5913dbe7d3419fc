{"category": "Changed", "changelog_entry": "Updated the CI job to correctly retrieve and utilize the current Go version from the official website, resolving an issue that could cause build failures due to outdated versions.", "summary": "The CI job update script was modified to correctly retrieve the Go version from the official website, ensuring that the `go-version` field in the GitHub Action workflow file matches the current Go version. This change fixes an issue where the Go version was not being updated correctly, potentially causing build failures. The migration step involves updating the `.github/workflows/update.yml` file with the new Go version.", "version": 1}
